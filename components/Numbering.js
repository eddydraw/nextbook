const Numbering = ({pageNumnber}) => {
    return (
        <div classNames = "flex" >
            <p className = "text-center" > {pageNumnber} </p>
        </div>
    );
};

export default Numbering;